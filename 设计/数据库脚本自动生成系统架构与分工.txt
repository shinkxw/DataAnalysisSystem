数据层：
元数据：
命名空间（在存储上体现为单个文件）
表（表名，中文说明，主键字段，注释字段）
字段（中文说明，字段格式，注释字段）
元素（字段，值，类型（测试，常驻））
表间关联关系（空间名，表名，关联字段）

视图：（待定）
引用表
引用字段
连接关系
表及视图依赖关系

配置层：（上层包括下层的设定，下层可以覆盖上层的设定）
元数据解析及输出配置
命名空间对应配置
表名对应配置
字段名对应配置

分析层：
元数据语言解释器（分版本,应带格式验证）
元数据差异比较（包括冲突解决）
元数据添加（从txt、sql脚本等）、修改、删除

加载层：
工作环境加载器（分版本，需配置属性解析器）

功能层：
生成sql脚本(可选择导出全库，全表，全数据，单文件，单表，单表数据)
生成辅助信息
生成MODEL代码
生成单表视图
生成指定多表视图（待定）
生成指定视图MODEL代码（待定）
元数据简单条件查询及改动

测试层：
文本差异检测，自动检测与上一个版本的差异并显示，可以选择回滚

工具层：
自动整理元数据
元数据自动备份及记录数据库改动功能
元数据冗余及格式检测
sql脚本及其他字符串形式转换为元数据
元数据可选范围的修改，包括字段属性及关联信息
生成容易理解的表及关联信息
测试数据生成与管理